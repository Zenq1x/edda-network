[package]
name    = "edda-consensus"
version = "0.1.0"
edition = "2021"

[lib]
doctest = false

[dependencies]
edda-core = { path = "../edda-core" }
serde     = { workspace = true }
bincode   = { workspace = true }
